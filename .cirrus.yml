task:
  aws_credentials:
    role_arn: arn:aws:iam::944424729675:role/VetuIntegrationTests
    role_session_name: cirrus
    region: us-east-2
  ec2_instance:
    image: ubuntu/images/hvm-ssd/ubuntu-jammy-22.04-arm64-server-*
    architecture: arm64
    region: us-east-2
    type: m5.large
  optimistic_cache:
    folder: test
    fingerprint_script: echo $RANDOM
    populate_script: mkdir -p test && echo $RANDOM > test/random.txt
    optimistically_restore_on_miss: true
